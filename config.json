{
  "info": [
    {
      "note": "js is bad",
      "scope": ["js", "ts"],
      "pattern": { "type": "regex", "thingToLookFor": "js" }
    },
    {
      "note": "dont use console.log",
      "scope": ["js", "ts"],
      "pattern": { "type": "regex", "thingToLookFor": "1 1" }
    },
    {
      "note": "koko",
      "scope": ["js", "ts"],
      "pattern": {
        "type": "regex",
        "thingToLookFor": "(?:\\+?\\d{1,3})?[-.\\s]?(\\(?\\d{3}\\)?|\\d{3})[-.\\s]?\\d{3}[-.\\s]?\\d{4}"
      },
      "id": 679620
    },
    {
      "note": "no json !!!",
      "scope": ["js", "ts"],
      "pattern": { "type": "regex", "thingToLookFor": "JSON" },
      "id": 88843
    },
    {
      "note": "no lolos",
      "scope": ["js", "ts"],
      "pattern": { "type": "regex", "thingToLookFor": "lolo" }
    },
    {
      "note": "o o o  o",
      "scope": ["js", "ts"],
      "pattern": {
        "type": "custom",
        "thingToLookFor": "function searchString(input) {\n      const positions = [];\n      let match;\n      const regex = /o/g;\n      while (match = regex.exec(input)) {\n        positions.push({ start: match.index, end: match.index + 1 });\n      }\n      return positions;\n    }\n"
      }
    }
  ]
}
